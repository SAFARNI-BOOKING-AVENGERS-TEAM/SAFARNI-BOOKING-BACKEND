apiVersion: v1
kind: Service
metadata:
  name: safarni-frontend
  namespace: safarni
  labels:
    app.kubernetes.io/name: safarni-frontend
    app.kubernetes.io/part-of: safarni
spec:
  type: ClusterIP
  selector:
    app: safarni-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
